require 'spec_helper'

describe Vaporzone do
  it 'has a version number' do
    expect(Vaporzone::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
